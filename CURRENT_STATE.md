# OX v4 市場施工盤點（2026-09-24）

## 2026-09-25 線上預覽驗證

已對 `0a818d8` 的 Vercel 預覽做 500×730 瀏覽器操作：預設加密雷達／深色、媒體入口、新聞載入、市場／來源篩選、已讀／未讀、返回媒體與瀏覽器前進成功，所查畫面沒有水平溢出。發現「即將公布」倒序，以及新聞進入完整設定後仍保留新聞底部列，已修正最近事件優先與退出新聞模式的導覽狀態，新增歷史導覽回歸測試。`npm test` 20 項與 `npm run check` 通過。完整多尺寸、30–50 次 fixture 切換與手機實機流暢度尚未驗收。

## 2026-09-25 新聞互動後續

`fix/initial-navigation-performance` 的四個既有提交已上傳 GitHub，遠端 HEAD `d841c33`；`main` 保持 `f59c89d`。接續修正新聞篩選面板更新後自行關閉、跨市場新聞欠缺市場／來源選擇、已讀狀態與返回新聞頁的瀏覽器導覽狀態。新聞仍是手動產生的官方來源快照，頁面改為明示此限制。未做手機實機滑動與多尺寸 E2E 驗收；美股、台股、外匯資料擴充暫緩。此段修改的提交與遠端狀態以最新 git 紀錄為準。

## 2026-09-25 後續：優先媒體消息

本輪依最新指示，台股個股 K 線 UI、美股全市場雷達與外匯盤中資料暫緩；保留四市場原有切換。已從落後的新聞草稿逐檔移植官方 RSS／BLS 日曆收集器及快照、數據頁市場新聞、獨立跨市場新聞模式、雷達長按「新聞」入口和媒體頁連結。新聞模式不寫入金融市場 enum，能返回原市場／原頁面／捲動位置；底部五個新聞功能入口已接線。卡片只顯示官方來源標題、日期、來源、原文與市場標籤；標題關鍵字評星改為待評估，未提供未經核實的分析摘要或價格因果。

2026-09-25 手動收集：Fed／ECB／SEC 成功，BLS 的兩個 RSS、BLS 行事曆及 Ethereum 逾時，失敗來源沿用舊快照並在頁面顯示來源失敗。這是定時生成的快照，尚未設定自動更新排程。`npm test` 19 項與 `npm run check` 通過；本機瀏覽器因 Chromium 缺失、雲端瀏覽器阻擋 localhost，尚未實測滑動與切換效能，也未完成指定的多尺寸 E2E。遠端 GitHub 尚未成功推送本分支。

## 2026-09-25 施工分支：首屏與切換回應

`fix/initial-navigation-performance` 從正式 `main` 的 `f59c89d` 建立。已移除雷達卡片 class 監看造成的重排程循環、重複雷達點擊處理器和切頁的 105ms 等待；首次觸控捲動不再啟動音效或執行全頁版面診斷。新畫面先顯示，再排入各市場的渲染。每次重新載入預設加密雷達與深色主題；底部為首頁／指標／雷達／數據／媒體，設定從右上選單進入。數據頁目前只有市場資料能力與限制說明，新聞及行事曆尚未完成。雷達長按後放手可保留選單三秒，仍只包含四個已接入市場。

`npm test` 15 項及 `npm run check` 已通過。此環境沒有 Chromium，Playwright 安裝下載失敗，尚未完成手機／電腦實機版面與切換驗證；此分支尚不可視為發布驗收或新聞功能完工。

正式 `main` 已部署 Crypto 手機雷達與 T1／T2／T3 各最多 10 檔、合計最多 30 檔，以及美股個股查價與手機版面。新聞草稿已備份至遠端 `feature/ox-news`，未併入正式版；市場功能先行。

| 市場 | 已有能力 | 本輪確認的缺口與處置 |
| --- | --- | --- |
| 加密 | Home、強弱、雷達、圖表與帳號等既有功能 | 手機雷達圖表與榜單維持同畫面並排；T1／T2／T3 各最多 10 檔，合計最多 30 檔且不重複。後續只修可重現的回歸問題。 |
| 台股 | 官方 TWSE/TPEx 後端、Home、指標、雷達與個股入口 | 正式資料曾因 TPEx 欄名空白落入備援；修正後同日主來源已可取得。沒有歷史序列或個股法人資料的雷達欄位保持 unavailable。 |
| 美股 | Twelve Data 後端與 SPY/QQQ/IWM 行情，VIX 在可用時顯示 | 個股代號單筆報價查詢、手動更新、手機緊湊卡片已在本分支實作；NVDA 真後端請求已驗證。全市場 breadth、11 類股與個股 radar 仍是 501。現有方案每分鐘 8 credits，11 檔類股單次請求會回 502/額度不足；不能當成已接入。3 ETF 同向風險代理有明確資料邊界。 |
| 外匯 | Frankfurter/ECB 每日參考匯率、八大貨幣強弱及 11 組貨幣對 | 非即時報價，無成交量/點差/盤中 OHLC。已改用市場當地時間判斷夏令時，補收盤變動幅度，刷新失敗保留前次資料。 |

資料原則：台股以共同已完成交易日為準；美股 ETF 不冒充全市場漲跌家數；外匯每日參考匯率不冒充即時交易報價或 ATR。所有 API 密鑰僅留在後端。

後續順序：市場 UI 與資料來源驗收、手機與桌面視覺檢查、跨市場回歸，再接續新聞／數據中心。部署前要重新確認遠端 `main` 未被他人更新。
