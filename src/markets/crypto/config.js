// Metadata only: scoring constants remain in the preserved OX engine.
export const CRYPTO_MODULE_CONFIG = Object.freeze({
  id: "crypto",
  label: "加密貨幣",
  provider: "bitget-public",
  architecture: "preserved-classic-runtime"
});
