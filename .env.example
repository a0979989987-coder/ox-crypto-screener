# OX Account authentication is not connected in V1.
# Add provider credentials only on the server after choosing an Auth provider.
OX_AUTH_ISSUER=
OX_AUTH_CLIENT_ID=
OX_AUTH_CLIENT_SECRET=
OX_AUTH_SESSION_SECRET=
